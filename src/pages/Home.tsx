import LandingPage from "../components/LandingPage";

type Props = {};

const Home = (props: Props) => {
    return (
        <>
            <LandingPage />
        </>
    );
};

export default Home;
