import React from "react";

type Props = {};

const chooseTemplates = (props: Props) => {
    return <h1>Create your resume. Let's start with selecting a template</h1>;
};

export default chooseTemplates;
